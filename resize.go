package main

import (
	"image"
	// "image/color"
)

func ScaleImage(img image.Image, r float64) int {
	// var dx, dy int = GetImageBounds(img)
	// var w, h int = 0, 0
	// w = int(float64(dx) * r)
	// h = int(float64(dy) * r)
	// var canvas image.RGBA = CreateCanvas(w, h)
	// for i := 0; i < w; i++ {
	// 	for j := 0; j < h; j++ {
	// 		r, g, b, a := GetUint8RGBA(img, i, j)
	// 		canvas.SetRGBA(i, j, color)
	// 	}
	// }
	return 1
}
